import { StyleSheet } from 'react-native';
import { FlightCard } from '../../../AppStyles';
FlightCard

const styles = StyleSheet.create({
    container: FlightCard.container,
    photo: FlightCard.photo,
    title: FlightCard.title,
    category: FlightCard.category,
    button: FlightCard.button,
    textButton: FlightCard.textButton
});

export default styles;